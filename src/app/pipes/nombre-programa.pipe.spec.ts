import { NombreProgramaPipe } from './nombre-programa.pipe';

describe('NombreProgramaPipe', () => {
  it('create an instance', () => {
    const pipe = new NombreProgramaPipe();
    expect(pipe).toBeTruthy();
  });
});
